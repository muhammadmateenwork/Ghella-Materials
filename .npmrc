# Expo/React Native's Metro bundler works most reliably with a flat
# node_modules layout. Packages are still hard-linked from pnpm's global
# store, so nothing is duplicated on disk.
node-linker=hoisted
# Resolve "@ghella/shared": "*" to the local workspace package instead of
# the npm registry.
link-workspace-packages=true
